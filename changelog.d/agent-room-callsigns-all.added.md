- **Agent Room now exposes member callsigns and controlled `@all`.**
  Members whose display name differs from their mention token show both, wakeable status is explicit, and operator-origin `@all` previews every agent that will be notified.
