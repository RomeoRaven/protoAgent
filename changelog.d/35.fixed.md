- **ACP delegate deletion now closes its cached subprocess (#35).**
  Removing an ACP delegate deterministically tears down its live client before deleting configuration, and deleting the last delegate restores the true no-delegate config state without disturbing unrelated settings.
