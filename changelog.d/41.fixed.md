- **Native Agent Room now fills its available panel height (#41).**
  The Rooms rail surface uses the full AppShell column so message history can use the window and the composer stays anchored above the utility bar; the command-palette Fleet Room keeps its compact fixed height.
