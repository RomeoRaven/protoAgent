- **Add the first human-approved `safe-operator` execution path.** HTTP-mode operator MCP now
  exposes one exact-input, expiring `knowledge_ingest` tool backed by a bearer-gated human
  approval route, operation-specific postcondition receipts, and verified rollback on failure.
  Stdio remains closed and explicit tool names cannot widen the managed profile.
