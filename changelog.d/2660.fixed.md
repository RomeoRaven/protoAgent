- **Goal and Watch data verifiers now resolve relative paths under the managed workspace (#2660).**\n  UI-created checks see the same files as the default structured filesystem tools, while untrusted chat verifiers cannot use absolute or escaping paths.\n