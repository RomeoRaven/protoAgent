- **Plugins can handle declared A2A skills deterministically and invoke thread-scoped ACP delegates (#36).**
  Matching skill requests can bypass the model loop while retaining durable A2A task state, and server plugins can dispatch a named ACP delegate with an isolated persistent conversation key.
