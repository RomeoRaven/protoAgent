- **The operation catalog now reports explicit safety risk.** Every registered operation must declare `read`, `reversible`, `disruptive`, or `destructive`; `protoagent operations` and `GET /api/operations` expose the classification while retaining the derived `mutates` field for compatibility. This metadata grants no new execution authority.
