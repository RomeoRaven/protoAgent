- **Rooms now has a first-class native rail entry (#39).**
  The new Rooms icon opens the canonical shared Agent Room directly while keeping the command-palette shortcut; when no Room backend is installed, the surface fails closed with setup guidance instead of broadcasting through Fleet messaging.
