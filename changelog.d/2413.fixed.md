- **Windows-native shell execution and desktop storage paths no longer rely on POSIX-only behavior (#2413).**
  Timed-out command trees are terminated on Windows, filesystem commands use the native shell, and desktop data-version state respects the configured box root.
