- **Safe-operator consent now has a fail-closed core.** Per-run grants bind one canonical operation, target, exact inputs, target state, expected effect, and expiration; single-use admission and operation-specific postcondition checks produce secret-free receipts without exposing an execution surface. (#18)
