- **RomeoRaven now carries upstream protoAgent v0.146.0 while preserving the native Agent Room UI and development bridge (#45).**
  The sync brings in federation-authenticated plugin routes, host-enforced scoped delegate invocation, ACP session compatibility fixes, and the v0.146.0 Fleet/console improvements; deterministic Room A2A handling remains only as a temporary development bridge until the separately gated Agent Room migration.
