- **Fleet Room can use the canonical Agent Room backend without broadcasting plain messages (#37).**
  When the backend plugin is installed, the existing native surface renders ordered room history and membership, posts and acknowledges plain no-wake updates, and fails safely while backend status is unknown; plugin-absent hosts keep the existing Fleet Room behavior.
