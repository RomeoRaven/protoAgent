- **`protoagent doctor` reports read-only offline environment readiness (#16).**
  Operators, installers, CI, and support flows can inspect platform identity, resolved paths, config/runtime requirements, loopback-port availability, plugin compatibility, and plugin-lock consistency through human or stable schema-v1 JSON output without repair actions, secret disclosure, external calls, or persistent writes.
