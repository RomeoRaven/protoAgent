- **Offline runtime readiness is now one command.** `protoagent doctor` provides human-readable and JSON reports for read-only checks of layered configuration, runtime adapters, paths, loopback ports, plugin compatibility, and plugin-lock consistency. (#16)
